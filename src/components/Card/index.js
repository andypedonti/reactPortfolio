import React from "react";
import "./style.css";

function Card({ children }) {
    return (
        <div class="card mt-2">
            {children}






        </div>

    );
}

export default Card;